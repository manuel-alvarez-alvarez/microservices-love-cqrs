server:
  port: 8089

spring:
  application:
    name: catalog
  jpa:
    hibernate:
      ddl-auto: none

  cloud:
    stream:
      kafka:
        binder:
          headers: type
      bindings:
        input:
          group: catalog
          destination: processor
          consumer:
            resetOffsets: true
          contentType: application/json
        output:
          destination: processor
          contentType: application/json


eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8090/eureka/

