server:
  port: 8086

spring:
  application:
    name: shipping
  jpa:
    hibernate:
      ddl-auto: none

  cloud:
    stream:
      kafka:
        binder:
          headers: type
      bindings:
        input:
          group: shipping
          destination: processor
          consumer:
            resetOffsets: true
          contentType: application/json
        output:
          destination: processor
          contentType: application/json

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8090/eureka/